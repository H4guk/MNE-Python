# MNE Python チュートリアル

## はじめに

**MNE Python**とは、
脳波（EEG）、脳磁図（MEG）、その他の電気生理学的データの処理、分析、可視化のためのオープンソースのPythonパッケージ
脳波解析などで重要な役割を持つ。
大まかな流れ
データの読み取り、ノイズの削除、データの可視化、その後機械学習で分析する。

## MNE Pythonの基本的な使い方

### 脳波データの読み込み方法

利用可能なフォーマットは、以下のものである

- .edf, .bdf, .gdf (European Data Format)
- .cnt (Neuroscan CNT format)
- .fif (Elekta Neuromag FIF format)
- .brainvision (BrainVision format)

データの読み込みには、**mne.ioモジュールの関数**を使用します。
使用例はtest.jpynbに示す。

    <エラーについて>
    ・ValueError: Bad EDF file provided.
     → EDF形式として読み取れない、中身がおかしい時に起こる
     解決策
     ファイルサイズの確認などでedf形式か確認し、正しくedfファイルをダウンロードする。

### 脳波データの前処理（フィルタリング、アーチファクト除去など）

脳波データの前処理では、以下の手法がよく使われる。

- フィルタリング
- リサンプリング
- アーチファクトの除去

**フィルタリング**

フィルタリングでは、以下の3つが行われる。

- ハイパスフィルタ：低周波数のノイズを除去（**raw.filter()**）
- ローパスフィルタ：高周波数のノイズを除去（**raw.filter()**）
- ノッチフィルタ：特定の周波数（電源ノイズなど）を除去（**raw.notch_filter()**）

これらによってデータのノイズを削除する。
サンプルプログラムはtest.jpynbに示す。

    <エラーについて>
    ・name 'picks' is not defined
     → 'picks'が定義されていないことを示す
    解決策
    定義する
    picks = mne.pick_types(raw.info, eeg=True)

    ・h_freq ([49.375 99.25 ]) must be less than the Nyquist frequency 50.0
     → フィルタの上限周波数（h_freq）がナイキスト周波数を超えてる
    解決策
    ナイキスト周波数を確認してその値より小さい値にしておく

**リサンプリング**

フィルタリングでは、以下が行われる

- **raw.resample()関数**を使用して、データのサンプリングレートを変更する。

これにより，データのレートを変更することができる。
サンプルプログラムはtest.jpynbに示す。

**アーチファクトの除去**

アーチファクトの除去では、以下の3つが行われる。

- EOG（眼電図）アーチファクトの除去：mne.preprocessing.ICAクラスを使用
- 心電図（ECG）アーチファクトの除去：mne.preprocessing.ICAクラスを使用
- 筋電図（EMG）アーチファクトの除去：mne.preprocessing.ICAクラスまたはmne.filter()関数を使用

これにより、EEG以外の波形のノイズが除去することができる。
今回はEEGを用いたため別途サンプルプログラムを確認する．

### 脳波データの可視化（波形表示、トポグラフィー表示など）

MNE Pythonでは、以下の3つの可視化手法が利用できます。

- 波形表示
- トポグラフィー表示
- 時間周波数分析

**波形表示**

波形表示では、以下の手順でデータの可視化が行われる

- **raw.plot()関数**を使用して、脳波データの波形を表示できる。

サンプルプログラムはtest.jpynbに示す。

**トポグラフィー表示**



**時間周波数分析**

時間周波数分析では、以下の手順でデータの可視化が行われる

- **mne.time_frequency.tfr_morlet()関数**を使用して、脳波データの時間周波数解析を行い、**mne.viz.plot_tfr()関数**で結果を可視化できる。

サンプルプログラムはtest.jpynbに示す。

## MNE Pythonを使った機械学習の流れ

