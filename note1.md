# MNE Python

## 1章 入門チュートリアル

**MNE Python**とは、
脳波（EEG）、脳磁図（MEG）、その他の電気生理学的データの処理、分析、可視化のためのオープンソースのPythonパッケージ脳波解析などで重要な役割を持つ。大まかなデータの処理の流れとしてデータの読み取り、ノイズの削除、データの可視化、その後機械学習で分析するといった流れがある。

### 1,MNE-PythonによるMEG/EEG解析の概要

#### データの読み込み
MNE-Pythonはさまざまなデータ形式に対応できる。対応するデータ形式を以下に示す。

- .edf, .bdf, .gdf (European Data Format)
- .cnt (Neuroscan CNT format)
- .fif (Elekta Neuromag FIF format)
- .brainvision (BrainVision format)

※ 本チュートリアルでは、FIFファイルを用いている。

MEG/EEG解析は、まずデータを読み込むことから始める。
データの読み込みは<u>tutorial01_01.ipynb</u>のプログラム1にて行われる。
**mne.datasets.sample.data_path()関数**は、サンプルデータセットのローカルコピーへのパスを取得できる。
想定される場所のいずれにもデータセットが見つからない場合、自動的にデータセットをダウンロードし、データセットへのディレクトリパスを返します。
**mne.io.read_raw_fif()関数**は、生のFIFデータを読み取る機能を持つ。
これによりデータの読み取り、および情報を取得できる。

また、より詳しい情報を取得する場合は、<u>tutorial01_01.ipynb</u>のプログラム2にて取得できる。
これは、データを読み取ったrawをプリントすることで取得している
.infoに関しては、<u>note1.md</u>の4,Infoデータ構造にて説明する。

rawオブジェクトには、さまざまなプロット形式がある。
ここでは、<u>tutorial01_01.ipynb</u>のプログラム3にて示した2つの関数を説明する
**raw.compute_psd()関数**は、センサーデータに対してスペクトル解析を実行する関数である。
**raw.plot()関数**は、生データをプロットする関数である。
このステップはデータの可視化にあたる

#### 前処理
MNE-Python は、さまざまな前処理アプローチがある。
データの前処理では、以下のアプローチがよく使われる。

- フィルタリング
- リサンプリング
- 独立成分分析(ICA)

**フィルタリング**

フィルタリングでは、以下の3つの処理を行う。

- ハイパスフィルタ：低周波数のノイズを除去
- ローパスフィルタ：高周波数のノイズを除去
- ノッチフィルタ：特定の周波数（電源ノイズなど）を除去

ハイパスフィルタ及びローパスフィルタでは**raw.filter()関数**を利用する。この関数は、低周波数のノイズ及び高周波数のノイズを除去することができる。
ノッチフィルタでは**raw.notch_filter()関数**を利用する。この関数は特定の周波数（電源ノイズなど）を除去することができる。
以下にサンプルコードを示す。

```Python
picks = mne.pick_types(raw.info, eeg=True)

# ハイパスフィルタ（1Hz以下をカット）
raw.filter(1, None, fir_design='firwin')

# ローパスフィルタ（40Hz以上をカット）
raw.filter(None, 40, fir_design='firwin')

# ノッチフィルタ（49Hzを除去）
raw.notch_filter(freqs=[49], picks=picks, method='fir')
```

**リサンプリング**

フィルタリングでは、**raw.resample()関数**を使用して、データのサンプリングレートを変更する。
以下にサンプルコードを示す。

````
# 250Hzにリサンプリング
raw.resample(250, npad="auto")
````

**独立成分分析(ICA)**

本チュートリアルでは、独立成分分析(ICA)を用いてデータの処理を行なっている。
独立成分分析(ICA)は、混ざり合った複数の信号（データ）を、それぞれの発生源である「互いに独立な成分」に分離する多変量解析手法である。
具体的な手順は4章にて示すため、ここでは簡単に説明する。

<u>tutorial01_01.ipynb</u>のプログラム4では，ICA（独立成分分析）でノイズ除去を行なっている。
ここでは、**mne.preprocessing.ICA()関数**の説明をしよう。この関数は、ICAの準備を行なっている。具体的には、分解する成分の数、再現性、試行回数を指定している。
<u>tutorial01_01.ipynb</u>のプログラム5では，データにICA（独立成分分析）を適用してデータのノイズを除去している。
ここでは、**raw.load_data()関数**と**ica.apply(raw)関数**について説明する。この関数はデータのロードとicaの適用を行なっている。

#### 実験イベントの検出

データセットには、刺激伝達コンピュータから送信された電気信号を記録した複数の「STIM」チャネル (パルス)が含まれる。
これらのパルスは、このデータセットで**実験イベント（刺激の開始、刺激の種類、参加者の反応（ボタンの押下））**をマークするために使用される。
この「STIM」チャネルを調べることで実験イベントを検出することができる。

<u>tutorial01_01.ipynb</u>のプログラム6では、イベントの検出を行なっている。**mne.find_events()関数**は、生データからイベントを検索している。結果として得られる配列は通常の3列配列で、最初の列にサンプル番号、最後の列に整数型のイベントIDが格納される。
<u>tutorial01_01.ipynb</u>のプログラム7では、イベントの検出に伴ったイベント辞書を作成している。このイベント辞書はEpochs作成を行う時に使用する。
<u>tutorial01_01.ipynb</u>のプログラム8では、イベント検出が期待どおりに機能したことを確認するために、記録期間全体にわたるイベントの分布を視覚化している。これは**mne.viz.plot_events()関数**にて行われる。

#### 連続データのEpochs化

**Epochs**とは、あるイベントを基準に切り出した短い時間データのことである。
Raw オブジェクト（fif形式の生データ）とイベント配列は、Epochs化（Epochsクラスコンストラクタを使用してEpochsオブジェクトを作成する）に必要なものである。このようにしてできたEpochsは、刺激に対する脳の反応を見るために行う。
また、Epochs化を行うと同時にピーク間信号振幅がそのチャンネルタイプにとって妥当な範囲を超えているEpochsはすべて除外している。
Epochs化の手順を参考プログラムとともに説明する。

<u>tutorial01_01.ipynb</u>のプログラム9では、Epochs化で行われるノイズ（ピーク間信号振幅がそのチャンネルタイプにとって妥当な範囲を超えているもの）の除去の設定を行なっている。
<u>tutorial01_01.ipynb</u>のプログラム10では、Epochsオブジェクトの作成及び、設定した基準に沿ったノイズの除去を行なっている。これは、**mne.Epochs()関数**を用いる。
<u>tutorial01_01.ipynb</u>のプログラム11では、左右の刺激提示をまとめて、比較できるようにしている。これは**equalize_event_counts()関数**によって行われている。この関数は、各条件からエポックをランダムにサンプリングし、有効なエポックが最も少ない条件のエポック数に合わせる機能を持っている。これをはじめに使うことで、思考数を整え比較できるようにしている。
<u>tutorial01_01.ipynb</u>のプログラム12では、データのプロットを行なっている。Epochsオブジェクトも、Rawオブジェクトと同様にさまざまなプロット方法がある。本チュートリアルでは、**plot_image()関数**を用いている。この関数は、各エポックをイメージマップの1行として表示し、色で信号強度を表している。

#### 時間周波数解析

**時間周波数解析**とは、信号の周波数成分が時間とともにどう変化するかを分析する手法である。
時間周波数解析ツールは、時間周波数表現、パワースペクトル密度、およびクロススペクトル密度を計算するための複数のアルゴリズムの実装を提供する。基本的な手法として短時間フーリエ変換（STFT）や、ウェーブレット変換を用いる。

<u>tutorial01_01.ipynb</u>のプログラム13により、データの時間周波数表現を計算しプロットしている。この機能を実現しているのが、**aud_epochs.compute_tfr()関数**である。この関数によりエポック化されたデータの時間周波数表現を計算している。

#### 誘発応答の推定

**誘発応答**とは、外部刺激によって誘発される脳や神経の電気的活動（電位）のことである。
この値を調べることで、視覚、聴覚、体性感覚（触覚など）の経路が正常に機能しているか、脳幹部や神経経路の病変を検査することができる。この誘発応答の推定値は、各条件のエポックを平均化することで、聴覚刺激と視覚刺激に対する誘発応答の推定値を得ることができます。ここで各条件は、実験のパターン（反応の種類）ごとのデータを示す。

<u>tutorial01_01.ipynb</u>のプログラム14に示すもので、誘発応答の推定値を得ることができる。**mne.viz.plot_compare_evokeds()関数**は、1つ以上の条件およびチャネルについて、誘発された時間経過をプロットできる。これは、Epochs オブジェクトに対して average メソッドを呼び出した後に利用することで使用できる。
また、<u>tutorial01_01.ipynb</u>のプログラム15に示すように、**plot_joint()関数** や**plot_topomap()関数** などの他のプロット手法を使用することで、各誘発オブジェクトについてより詳細な情報を得ることができます。
さらに、<u>tutorial01_01.ipynb</u>のプログラム16に示すように、**mne.combine_evoked()関数**を使用することで、誘発波形を組み合わせ、条件間の対比を示すこともできる。weights=[1, -1] を指定することで、単純な差分波形を生成でき、その後**plot_topo()関数**を使用して、各センサーにおける差分波形をプロットしている。

#### 逆モデリング

**逆モデリング**とは、観測結果からその原因となるパラメータや構造を推定する手法である。
これは、センサーデータをこの被験者のソース空間（構造MRIスキャンによって推定された、被験者の皮質表面上または皮質体積内の点の集合）に投影することで、誘発活動の起源を推定できます。MNE-Pythonの逆モデリングの手法には、以下の手法がある。

- 動的統計的パラメトリックマッピング
- 双極子フィッティング
- ビームフォーマー

※ 本チュートリアルでは、最小ノルム推定（MNE）を用いている。

**最小ノルム推定（MNE）**とは、線形逆演算子を使用して、EEG+MEGセンサー測定値をソース空間に投影する手法です。これにより皮質表面に制約された活性化の連続マップを生成できる。この処理は、<u>tutorial01_01.ipynb</u>のプログラム17に示す。このプログラムでは、**mne.minimum_norm.read_inverse_operator()関数**によりFIFファイルから逆演算子分解を読み込む処理を行い、後に**mne.minimum_norm.apply_inverse()関数**で誘発データに逆演算子を演算している。
この結果をもとに、<u>tutorial01_01.ipynb</u>のプログラム18では、逆モデリングの結果を脳の3D表示としてプロットしてる。



### 2,データの変換

MNE-Python の多くのデータオブジェクト ( Raw、Epochs、Evokedなど) には、データをその場で変更するメソッドがある。これは、**大規模なデータセットを扱う場合に、計算に必要なコンピュータメモリの量を削減できるため**に行う。しかし、それが行われていることを認識していないと、予期しない結果につながる可能性があります。このチュートリアルでは、インプレース処理の例と、それを回避する方法とタイミングについて説明する。

#### 信号処理

ほとんどのMNE-Pythonデータオブジェクトには、ハイパス、ローパス、バンドパスフィルタ(**filter()関数**)、バンドストップフィルタ(**notch_filter()関数**)、ヒルベルト変換(**apply_hilbert()関数**)など、信号処理(**フィルタリング**)のための組み込みメソッドがあります。これらは通常、データをその場で変更するため、比較のために未処理のデータを保持したい場合は、まずそのコピーを作成する必要がある。
<u>tutorial01_02.ipynb</u>のプログラム1では、実際の信号処理のプログラミングの例を示す。ここでは、**raw.apply_hilbert()関数**を用いてヒルベルト変換をしている。

#### チャンネル選択

データをその場で変更するもう1つの手法は、**チャンネル選択手法**がある。
<u>tutorial01_02.ipynb</u>のプログラム2では、実際の信号処理のプログラミングの例を示す。ここでは、**original_raw.pick()関数**にてチャンネルを変換している。また、EEGチャンネルのみを選択する場合、磁力計チャンネルがなくなるため、磁力計のみに影響を与えるプロジェクターは除外されるため注意が必要となる。

#### コピーパラメータ

信号処理では、処理前後のデータの比較のために前データのコピーを作成する必要があると示した。しかし、MNE-Python の特定の関数には**copyパラメータ**としてこの機構が組み込まれている。具体的にはデータの変更済みコピーを返す**copy=True**または、その場で操作する**copy=False**を指定できる。
<u>tutorial01_02.ipynb</u>のプログラム3では、実際のcopyパラメータを利用した例を示す。ここでは、copyパラメータが組み込まれている**mne.set_eeg_reference()関数**を用いた。これはリファレンス処理を行なったデータをプロットしている。しかし、copy=Trueを指定しているため、リファレンス処理を行なったデータには影響が及んでいない。

分析の探索段階で、さまざまなデータクレンジング手法の効果を試してみたい場合がある。計算を変更するたびにデータを再読み込みしたり、以前の手順を繰り返したりする手間を省きたい場合は、**raw.copy().filter(...).plot()**や**raw.copy().apply_proj().compute_psd().plot()**といったパターンを念頭に置いとくことが必要である。



### 3,生データからイベントを解析

<u>1,MNE-PythonによるMEG/EEG解析の概要</u>の実験イベントの検出では、STIMチャネルから実験イベントを読み取る例を見ていた。
ここでは、イベントとアノテーションについてより広く説明し、STIMチャネルからの読み取りに関する詳細情報を提供するとともに、マーカーファイルにあるイベント、またはデータファイルに埋め込み配列として含まれているイベントを読み取る例を示す。

#### イベントとアノテーションのデータ構造

一般的に、**イベント**と**アノテーション**というデータ構造は、どちらも同じ目的を持っている。これらは、EEG/MEG記録中の時刻と、その時刻に何が起こったかという記述とを関連付けるものである。ここから、イベントとアノテーションの違いについてみていく。

|項目|イベント|アノテーション|
|:---|:---:|:---:|
|単位|「いつ」をサンプル単位で表現する|「いつ」を秒単位で表現する|
|記述の制限|「何」を整数である「Event ID」コードで表現する|「何」を文字列で表現する|
|持続時間の表現方法|Event配列内のイベントには持続時間が含まれていない|Annotationsオブジェクトの各要素には必ず持続時間が含まれている|
|内部表現|通常のNumPy配列として格納される|MNE-Pythonで定義されたリストのようなクラス|

#### STIMチャンネルとは

**STIMチャンネル（「刺激チャンネル」の略）**とは、EEG、MEG、またはその他のセンサーからの信号を受信しないチャンネルである。
代わりに、STIMチャンネルは、**刺激の開始や被験者によるボタン押下反応などの実験イベントに同期した電圧**（通常は実験制御コンピューターから送信される、一定の振幅を持つ短い矩形直流パルス）を記録する（これらのパルスは、 TTLパルス、イベントパルス、トリガー信号、または単に「トリガー」と呼ばれることもある）。場合によっては、これらのパルスは厳密には実験イベントに同期せず、次の試行で発生する刺激の種類（または実験条件）を示すために、試行間に発生することもある。

**DCパルス**とは、電圧のハイとローを高速で切り替え、一定間隔でオン／オフを繰り返す直流電力である。
このDCパルスが、すべて1つのSTIMチャネルに集中している場合もあれば、複数のチャネルに分散している場合もある。前者の場合、異なる実験イベントや試行タイプは異なる電圧の大きさとして符号化され、後者の場合、パルスが発生するチャネルを利用して、異なるイベントや条件を符号化することができる。複数のSTIMチャンネルを備えたシステムであっても、デコードを行うために、他のSTIMチャンネルの重み付けされた合計を記録するチャンネルが1つ設けられていることが多く、そのチャンネル上の電圧レベルを特定のイベントタイプとして明確にデコードできるようにしている。
また、旧型のNeuromagシステム（サンプルデータの記録に使用されたものなど）では、この「加算チャンネル」は通常STI 014でしたが、新しいシステムではSTI101が一般的である。
<u>tutorial01_03.ipynb</u>のプログラム1では、実際に生データファイルのSTIMチャンネルを確認することができる。この結果から、STI 014（加算チャンネル）には大きさの異なるパルスが含まれているのに対し、他のチャンネルのパルスは一貫した大きさであることがわかる。また、他のSTIMチャンネルのいずれかにパルスが発生するたびに、STI 014にも対応するパルスが発生していることも確認できる。

#### STIMチャネル信号をイベント配列に変換する

データにSTIMチャネルで記録されたイベントが含まれている場合、**find_events()関数**を使用してそれらをイベント配列に変換できます。
各パルスのオンセット（またはオフセット）のサンプル番号がイベント時刻として記録され、パルスの振幅は整数に変換される。これらのサンプル番号とイベント整数コードのペアは、**NumPy配列**（通常「**イベント配列**」または単に「**イベント**」と呼ばれます）に格納される。この関数の最も単純な形式では、Rawオブジェクトと、イベントを読み取るチャネルの名前のみで利用できる。
<u>tutorial01_03.ipynb</u>のプログラム2では、STIMチャンネルで記録されたイベントをイベント配列に変換している。この結果から左の列にサンプル番号、右の列にイベント整数コードがペアとなって格納されていることがわかる。

    ＜イベント配列の中央の列＞
    MNE-Pythonイベントは実際には3つの値で構成されていて、サンプル番号と整数イベントコードの間に**0**が格納されていることが確認できる。
    この値は、直前のサンプルにおけるイベントコードを示す値である。実際には、この値はほぼ常に「0」ですが、
    1つのサンプルよりも長い期間のイベントの終了点を検出するために使用される。

STIMチャネルの名前を指定しない場合、まず MNE-Pythonの設定変数**MNE_STIM_CHANNEL**、**MNE_STIM_CHANNEL_1**などを検索する。これらが存在しない場合、チャネル**STI014**および**STI101**が試され、続いて**raw.ch_names**に存在するタイプ「STIM」の最初のチャネルが試されます。
STIMチャンネル名が異なる複数のMEGシステムからのデータを日常的に扱う場合、MNE_STIM_CHANNEL設定変数を設定することでデータがすべて単一のシステムからのものである研究者にとっては、その後は気にする必要がなくなり、時間の節約になることがある。

**find_events()関数**には、イベントをSTIMチャネルのパルスの開始点または終了点に合わせたり、パルスの最小持続時間を設定したり、連続するパルス（パルス間のゼロ復帰がない場合）を処理したりするためのオプションなど、いくつかのオプションがある。例えば、find_events に output=『step』 を渡すことで、イベントの持続時間を効果的にエンコードすることができます。

#### 埋め込みイベントを注釈として読み込む

一般的にEEG/MEGシステムでは、イベントが1つ以上のSTIMチャネル上のパルスとして保存される。しかし、一部のEEG/MEGシステムでは、別のデータ配列に格納されたファイルが生成される形で保存されている。
例えば、**EEGLAB形式**では、イベントが.setファイル内の配列の集合として保存される。これらのファイルを読み込む際、MNE-Pythonは保存されたイベントを自動的にAnnotationsオブジェクトに変換し、それをRawオブジェクトのannotations属性として保存することでファイルを読み込むことができる。
<u>tutorial01_03.ipynb</u>のプログラム3では、EEGLAB形式の脳波データを読み込んで、アノテーションを確認している。**mne.datasets.testing.data_path()関数**では、MNEのテスト用データの保存場所を取得している。**read_raw_eeglab()関数**では、EEGLABのデータをMNEで使える形にしている。**print(eeglab_raw.annotations)**では、データに付いているアノテーションを表示している。

Annotations オブジェクトの主要なデータには、**onset**、**duration**、**description**の3つの属性を通じてアクセスできる。
<u>tutorial01_03.ipynb</u>のプログラム4では、Annotations の中身を詳しく確認している。ここでは、EEGLABファイルに154件のイベントが保存されており、それらの持続時間はすべて0秒で、2種類のイベントがあり、最初のイベントは記録開始から約1秒後に発生したことがわかる。

#### イベント配列とアノテーションオブジェクト間の変換

実験イベントがMNE-Pythonに読み込まれると、必要に応じてこれら2つの形式（Events配列またはAnnotationsオブジェクト）を簡単に相互変換できる。
例えば、連続データのエポック分けにEvents配列が必要な場合や、特定の注釈と重複するデータ範囲を自動的に除外する「注釈対応」機能を利用したい場合などに、この変換を行うことがあります。

AnnotationsオブジェクトをEvents配列に変換するには、アノテーションを含むRawファイルに対して**mne.events_from_annotations()関数**を使用します。この関数は、raw.annotations.descriptionの各一意の要素に整数のイベントIDを割り当て、説明と整数のイベントIDの対応付けとともに、派生したEvent配列を返します。
デフォルトでは、各アノテーションの開始時に 1 つのイベントが作成される。これは、events_from_annotationsのchunk_durationパラメータを調整することで変更でき、各アノテーションの範囲内で等間隔のイベントを作成できます。
<u>tutorial01_03.ipynb</u>のプログラム5では、**mne.events_from_annotations()関数**を使用して、AnnotationsオブジェクトをEvents配列に変換している。

どの整数が各一意の説明値にマッピングされるかを制御したい場合は、events_from_annotationsのイベントidパラメータとして、そのマッピングを指定した辞書を渡すことができます。この辞書は変更されることなく、event_dictとして返されます。なお、この event_dict は、Raw オブジェクトから Epochs を作成する際に使用できる。
<u>tutorial01_03.ipynb</u>のプログラム6では、アノテーションを Events に変換するときに、自分でイベント番号を決め、そのマッピングを指定した辞書を渡している。

Events配列からAnnotationsオブジェクトへの変換を行うには、整数のイベントIDと文字列の説明とのマッピングを作成し、**annotations_from_events**を使用してAnnotationsオブジェクトを構築し、**set_annotationsメソッド**を呼び出してRawオブジェクトにアノテーションを追加します。
また、オンセットが記録の開始時刻に対して適切に位置合わせされるために**orig_timeパラメータ**を渡す必要がある。
<u>tutorial01_03.ipynb</u>のプログラム7では、Events配列からAnnotationsオブジェクトへの変換を行っている。整数のイベントIDと文字列の説明とのマッピングを作成し、イベントに変換している。

このようにして変換されたデータを用いることで、生データをプロットする際にアノテーションが自動的に表示され、ラベル値に応じて色分けされる。

#### アノテーションごとに複数のイベントを作成する

前述のように、events_from_annotations の chunk_duration パラメータを使用することで、Annotations オブジェクトから等間隔のイベントを生成することができます。
例えば、Raw オブジェクト内に被験者がレム睡眠状態にあった時期を示すアノテーションがあり、そのデータ範囲に対して安静時解析を行いたいときに、エポックを生成し、さらに詳細な解析を行うことができます。
<u>tutorial01_03.ipynb</u>のプログラム9では、REM睡眠の区間をアノテーションとして作って、その区間を細かくeventsに分割している。ここでは、**mne.Annotations()関数**にて、REM睡眠の区間をアノテーションとして作っている。**set_annotations()関数**では、作成したアノテーションをrawデータに登録している。chunk_duration=1.5では、作成したアノテーションを1.5秒間隔で1分割してeventsを作る命令を行なっている。これは、**睡眠解析では一定間隔で解析する**ことが多いためである。
<u>tutorial01_03.ipynb</u>のプログラム10では、プログラム9の確認を行なっている。



### 4,Infoデータ構造

データ構造**mne.Info**は、さまざまな記録の詳細情報を管理し、Raw、Epochs、Evoked オブジェクトに関連付けられるデータ構造である。ここからはこの「mne.Info」について説明する。

前述したように、Rawオブジェクトが読み込まれると、Infoオブジェクトが自動的に作成され、raw.info属性に格納される。ただし、Infoオブジェクトを表示または編集するために、必ずしもRawオブジェクトを読み込む必要はない。<u>tutorial01_04.ipynb</u>のプログラム2で示す方法を用いることで、関連するすべての情報を独立したInfoオブジェクトとして抽出することができます。

<u>tutorial01_04.ipynb</u>のプログラム1では、raw.info属性に格納されていることが確認できる。
<u>tutorial01_04.ipynb</u>のプログラム2では、**mne.io.read_info()関数**を用いて、関連するすべての情報を独立したInfoオブジェクトとして抽出している。この関数は、ファイルから計測情報を読み込む機能を有しており、Infoオブジェクトを表示または編集する時に利用する。
<u>tutorial01_04.ipynb</u>のプログラム2にて示すように、Infoオブジェクトは以下のような多くの情報を追跡しています。

- 記録システム（ガントリー角度、HPIの詳細、センサーのデジタル化、チャンネル名など）
- 実験（プロジェクト名とID、被験者情報、記録日、実験者名またはIDなど）
- データ（サンプリング周波数、適用されたフィルタ周波数、不良チャンネル、プロジェクターなど）

#### Infoオブジェクトを照会する

InfoオブジェクトのフィールドはPythonの辞書のキーのように振る舞い、角括弧と文字列を使用することでフィールドの内容にアクセスできる。
<u>tutorial01_04.ipynb</u>のプログラム3では、実際にInfoオブジェクトのフィールドにアクセスしている。ここでは、Infoオブジェクト「ch_names」にアクセスしている。フィールドの検索をしたい場合、**info.keys()関数**を用いることで全てのフィールドを検索することができる。

また、ほとんどのフィールドにはint、float、またはリストのデータが含まれていますが、**chsフィールド**については特筆すべき点がある。このフィールドには、チャンネルが記録したデータ以外の、そのチャンネルに関するあらゆる情報を格納した辞書のリスト（チャンネルごとに1つの辞書）が含まれている。だが、MNE-Pythonの各種関数を使えば、自分で辞書のリストを反復処理するよりも、より簡潔に情報を抽出できるため、通常chsフィールドの詳細を掘り下げる必要はない。しかし、その中に何が含まれているかを知っておくと役立つ場合がある。
<u>tutorial01_04.ipynb</u>のプログラム4では、実際にchsフィールドにアクセスしている。ここで、**[0]**は、chsフィールドの1番目のチャネルを表しており、**.keys()**は、指定したチャネルが持つ項目名を一覧表示している。

#### チャネルのサブセットを取得する

チャンネル名と、そのチャンネルの測定値が格納されているデータ配列の行を識別する整数インデックスとの間で変換を行うことは、人が認識しやすい形でデータを関連付けるためしばしば脳波の解析において有用な手法である。特に、この作業には Info オブジェクトが役立ちます。
mne.Info オブジェクトを利用してチャンネルを選択する便利な関数として、**mne.pick_channels()関数** と **mne.pick_types()関数** の2つがある。**mne.pick_channels()関数** は、最低限、すべてのチャンネル名のリストと、含めるチャンネル名のリストを受け取る。また、含めるリストとして空のリストを指定し、代わりに除外するチャンネルを指定することも可能である。
<u>tutorial01_04.ipynb</u>のプログラム5では、実際に**mne.pick_channels()関数**の使用例を示す。ここでは、 ch_namesには含まれるincludeが、excludeには含まれないインデックスを返している。

**mne.pick_types()関数**は**mne.pick_channels()関数**とは異なり、チャンネル名だけではチャンネルのタイプを常に確実に特定できるとは限らない。そのため、**mne.pick_types()関数**には単なるチャンネル名のリストではなく Info オブジェクトが必要となり、各チャンネルタイプに対してブール型のキーワード引数が用意されている。デフォルトの動作では、MEG チャンネル（および存在する場合の MEG リファレンスチャンネル）のみを選択し、Info オブジェクトの bads フィールドで既に「不良」とマークされているチャンネルは除外します。したがって、EEG チャンネルインデックスをすべて取得するには、meg=False および exclude=[] を渡す必要がある。
<u>tutorial01_04.ipynb</u>のプログラム6では、実際に**mne.pick_types()関数**の使用例を示す。ここから、Info オブジェクト、meg=False および exclude=[]を用いていることがわかる。

**mne.pick_types()関数**の meg および fnirs パラメータは、ブール値だけでなく文字列も受け付ける。これにより、磁力計または勾配計のチャンネルのみを選択したり（meg=『mag』 または meg=『grad』 を使用）、オキシヘモグロビンまたはデオキシヘモグロビンのチャンネルのみを選択したり（それぞれ fnirs=『hbo』 または fnirs=『hbr』 を使用）することが可能である。

また、Info オブジェクトからチャンネルを選択する 3 つめの方法は、**mne.pick_channels_regexp()関数**を使用してチャンネル名に正規表現を適用することです。ここで、^ は文字列の先頭を表し、. 文字は任意の 1 文字に一致するため、EEG チャンネルと EOG チャンネルの両方が選択されます
<u>tutorial01_04.ipynb</u>のプログラム7では、実際に**mne.pick_channels_regexp()関数**の使用例を示す。ここでは、Infoオブジェクト「ch_names」にアクセスし、"^E.G"と指定することで、Eから始まりGが入っているチャンネルを選択している。

この関数は、10-20法に基づいて命名されたチャンネルに対して特に有用である。例えば、正中線を取得するために「z」で終わるすべてのチャンネルを選択したり、後頭部チャンネルを取得するために「O」で始まるすべてのチャンネルを選択したりする場合などに有用である。

    ＜注＞
    **mne.pick_channels()関数**と**mne.pick_channels_regexp()関数**はどちらもチャンネル名のリストを操作するため、
    info[『bads』] で「不良」とマークされたチャンネルを認識しない。誤って不良チャンネルを選択しないよう注意する必要がある。

#### チャネルタイプの情報を取得する

データ配列内のインデックスからチャンネルのタイプを知っておくと便利な場合がある。そのとき、Infoオブジェクトと1つの整数型チャンネルインデックスを引数とする**mne.channel_type()関数**を使用してチャネルタイプの情報を取得することが有効である。
<u>tutorial01_04.ipynb</u>のプログラム8では、**mne.channel_type()関数**の使用例を示す。

複数のチャンネルタイプを一度に取得するには、**mne.channel_type()関数**をリスト内包表記に組み込むか、Raw、Epochs、またはEvokedインスタンスの**get_channel_types()関数**を使用することで取得できる。
<u>tutorial01_04.ipynb</u>のプログラム9では、**get_channel_types()関数**の使用例を示す。

あるいは、**channel_indices_by_type()関数**を使用することで、データ内に存在するすべてのチャンネルタイプの全チャンネルのインデックスを取得できる。この関数は、キーとしてチャンネルタイプ、値としてチャンネルインデックスのリストを持つ辞書（dict）を返す。
<u>tutorial01_04.ipynb</u>のプログラム10では、**channel_indices_by_type()関数**の使用例を示す。

#### Infoオブジェクトからチャネルを削除する

Infoオブジェクト内のチャンネルの一部を削除して変更したい場合は、**mne.pick_info()関数**を使用して、残したいチャンネルを選択し、残りを除外することができる。また、IPythonでは**info**を用いることで見やすいHTML形式で表示することもできる。
<u>tutorial01_04.ipynb</u>のプログラム11では、**mne.pick_info()関数**により、eeg_indicesインデックスを指定し、残りを除外している。
また、<u>tutorial01_04.ipynb</u>のプログラム12では、**info**を用いてHTML形式でinfoオブジェクトの情報を表示している。

デフォルトでは、**pick_info()関数**は元のInfoオブジェクトを変更する前にコピーを作成する。オブジェクトをその場で変更したい場合は、copy=False パラメータを指定する。



### 5,センサー位置の操作

このセッションでは、センサーの位置を読み込んでプロットする方法、およびMNE-Pythonがセンサーの物理的な位置をどのように処理するかについて説明する。

#### モンタージュとレイアウトについて

**モンタージュ**には、3次元空間におけるセンサーの位置（x、y、z：単位はメートル）が含まれており、これを既存のEEG/MEGデータに割り当てることができる。脳に対するセンサーの位置を指定することで、モンタージュは順解法および逆推定の計算において重要な役割を果たす。

一方、**レイアウト**は、センサーの位置を理想化した2次元表現である。これらは主に、個々のセンサーのサブプロットをトポプロット上に配置したり、上空から見たセンサーのおおよその相対的な配置を示したりするために使用される。

    ＜注＞
    EEGデータのみを扱う場合は、レイアウトではなくモンタージュを使用することをお勧めされる。
    理想化されたモンタージュ（例：メーカーが提供するもの、または後述するMNE-Pythonに同梱されているもの）は、通常、テンプレートモンタージュと呼ばれる。

#### 組み込みモンタージュの操作

MEGシステムの生データには、MEGセンサーの3次元座標が含まれている。これらは読み込み時に、Rawオブジェクトのinfo属性に自動的に保存される。
一方、EEG電極の位置は頭部の形状の違いにより、はるかに変動が大きくなる。MNE-Pythonには多くのEEGシステム用の理想化された配置（「テンプレート配置」）が含まれており、**mne.channels.get_builtin_montages()関数**を使用することで、それらの概要を確認することができる。
<u>tutorial01_05.ipynb</u>のプログラム1では、**mne.channels.get_builtin_montages()関数**を使用して、MNE-Pythonに同梱されているすべての標準モンタージュの一覧を取得している。

これらの組み込みEEGモンタージュは、**mne.channels.make_standard_montage()関数**を使用することで読み込むことができる。
<u>tutorial01_05.ipynb</u>のプログラム2では、**mne.channels.make_standard_montage()関数**を使用して、MNE-Pythonに同梱されている汎用（組み込み）標準モンタージュを読み込んでいる。

またこのモンタージュオブジェクトには、センサーの位置を2Dまたは3Dで可視化するplotメソッドが用意されている。
<u>tutorial01_05.ipynb</u>のプログラム3では、センサーの位置を2Dまたは3Dで可視化している。ここでは、**easycap_montage.plot()関数**にてモンタージュ映像を構成している。また、strパラメータにて3D で可視化するように指示している。

モンタージュからのEEG電極（センサー）の位置情報の読み込みが完了すると、**raw.set_montage()関数**や**epochs.set_montage()関数**、 **evoked.set_montage()関数**などの**set_montageメソッド**を使用することでデータにモンタージュを適用できます。これは、EEGチャネル名がモンタージュ内のものと一致するデータでのみ機能する。
その後、**plot_sensors()メソッド**を使用してセンサー位置を可視化できる。
また、**set_montage()メソッド**にモンタージュ名を直接渡すことで、手動でのモンタージュ読み込み手順を省略することも可能になる。
<u>tutorial01_05.ipynb</u>のプログラム4では、SSVEPのEEGデータを読み込み、その電極位置（モンタージュ）を設定して可視化している。ここでは、**mne.datasets.ssvep.data_path()関数**を用いて、MNEに用意されている SSVEP データセットの保存場所を取得し、**mne.io.read_raw_brainvision()関数**にてBrain Vision EEGファイルを読み取っている。そして、**set_montage()メソッド**を用いてデータにモンタージュを適用しplotにて可視化している。

    ＜注＞
    **plot_sensors()関数**を使用して作成された図には、**easycap_montage.plot()関数**の結果よりもセンサーの数が少なくなっている。
    これは、モンタージュにはそのEEGシステムで定義されたすべてのチャンネルが含まれているのに対し、
    すべての記録が必ずしも利用可能なすべてのチャンネルを使用するわけではないためである。
    したがって、実際のEEGデータセットにモンタージュを適用する場合、データに実際に存在しないセンサーに関する情報は削除される。

#### EEGLABのような2Dセンサー位置のプロット

MNE-Pythonでは、デフォルトで頭部中心は基準点（フィデュシャルポイント）を用いて計算される。つまり、頭部円は、10–20EEGシステムで一般的に測定される位置（すなわち、T4/T8、T3/T7、Oz、Fpzの鼻根上部）ではなく、鼻根および耳の高さにおける頭囲を表しています。
10–20法（EEGLABでも使用されている）に基づいて頭円を描画したい場合は、**sphere=『eeglab』**を指定できる。
<u>tutorial01_05.ipynb</u>のプログラム5では、10–20法に基づいて頭円を描画し、そこにセンサーの位置をプロットしている。この時、ここで使用しているデータにはFpzチャンネルが含まれていないため、その推定位置は自動的に近似されている。

    `sphere` キーワードは多くの場所で利用可能である
    EEGトポグラフィおよびセンサー位置に関するすべてのMNEプロット関数は、`sphere`キーワード引数をサポートしており、センサーが頭円上に投影される方法を調整することが可能である。

    10-20法について
    **10-20法**とは、EEG（脳波）の電極を頭皮の上に配置するための標準的なルールであり、電極を誰が測ってもほぼ同じ位置になるように決めた国際的な配置方法である。

#### 2Dチャンネル投影を手動で制御する

2次元空間におけるチャネルの位置は、実際の3次元位置を球面に投影し、その球面を平面に投影することで得られる。デフォルトでは、原点を (0, 0, 0)（x, y, z 座標）とし、半径を 0.095 メートル（9.5 cm）とする球面が使用される。2Dでチャネルをプロットする関数（ここで使用している**plot関数**や、例えば**mne.viz.plot_topomap**など）のsphere引数に単一の値を指定することで、異なる半径の球を使用できる。
<u>tutorial01_05.ipynb</u>のプログラム6では、easycap_montage.plot()関数を用い、sphere引数を0.07に指定して、モンタージュ映像を構成している。

半径だけでなく球の原点も変更するには、sphere引数として(x, y, z, radius)のタプルを指定する。
<u>tutorial01_05.ipynb</u>のプログラム7では、プログラム6同様、easycap_montage.plot()関数を用い、sphere引数を(0.03, 0.02, 0.01, 0.075)に指定して、モンタージュ映像を構成している。

#### センサーのデジタル化ファイルの読み込み

サンプルデータでは、センサーの位置情報はすでにRawオブジェクトのinfo属性に含まれている。したがって、montage.plot()と同様の機能を持つ**plot_sensors()関数**を使用することで、Rawオブジェクトから直接センサーの位置をプロットすることができる。さらに、**plot_sensors()関数**では、チャネルの種類による選択、さまざまな方法でのチャネルの色分け（デフォルトでは、raw.info[『bads』] にリストされているチャネルは赤でプロットされます）、および既存の Matplotlib Axes オブジェクトへの描画がサポートされている。これにより、マルチパネル図のサブプロットとしてチャネルの位置を簡単に追加できる。
<u>tutorial01_05.ipynb</u>のプログラム8では、**plot_sensors()関数*を用いて不良と判定されたチャンネルを赤でプロットしモンタージュ映像をプロットしている。

また<u>tutorial01_05.ipynb</u>のプログラム8で作成した2Dトプマップから、サンプルデータセットにおけるEEGセンサー位置の不規則性が明らかになっている。これは、当該データセットのセンサー位置が、球体頭部モデルに基づく理想化されたセンサー位置ではなく、頭部上の実際のセンサー位置をデジタル化したものであるためである。そのためデジタル化デバイス（例：Polhemus Fastrak デジタイザー）に応じて、異なるモンタージュ読み取り関数を使用する必要があります。得られたモンタージュは、**set_montage()メソッド**の引数として渡すことで Raw オブジェクトに追加できます。読み込みが完了すると、montageオブジェクトのplot()メソッドで位置情報をプロットしたり、save()メソッドで保存したりできます。

    ＜注＞
    **set_montage()**でモンタージュを設定すると、測定情報は2か所（chsエントリとdigエントリ）で更新されます。
    digには、電極位置に加えて、HPI、フィデューシャル、または頭部形状のポイントが含まれる場合があることに注意する必要がある。

#### 3D表面レンダリングにおけるセンサーの可視化

matplotlibの代わりに3Dサーフェスレンダリングを使用して、MEGセンサーヘルメットの画像をレンダリングすることも可能である。これは、**mne.viz.plot_alignment()関数**を呼び出すことで実現できる。
<u>tutorial01_05.ipynb</u>のプログラム9では、**mne.viz.plot_alignment()関数**を用いてMEGセンサーヘルメットの画像を3Dで可視化している。

**plot_alignment()メソッド**はInfoオブジェクトを必要とする。また、surfacesパラメータに「head」、「outer_skull」、「brain」などのキーを持つ辞書を渡すことで、頭皮、頭蓋骨、脳のMRIサーフェスをレンダリングすることも可能である。このため、この関数は座標系の変換を評価するのに役立つ。

#### レイアウトファイルの操作

モンタージュと同様に、MNE-Pythonには多くのレイアウトファイルが付属している。これらはmne/channels/data/layoutsフォルダに保存されている。
<u>tutorial01_05.ipynb</u>のプログラム10では、MNEにあらかじめ用意されているレイアウトの一覧を表示している。

レイアウトファイルを読み込むには、**mne.channels.read_layout()関数**を使用する。その後、そのplotメソッドを使用してレイアウトを可視化できる。
<u>tutorial01_05.ipynb</u>のプログラム11では、**mne.channels.read_layout()関数**を用いてレイアウトファイルを読み込んでいる。これは、レイアウトを可視化をするために行なっている。

Rawオブジェクトからチャンネルを選択するためのpicks引数と同様に、Layoutオブジェクトのplot()メソッドにもpicks引数がある。ただし、レイアウトにはセンサー名と位置に関する情報のみが含まれており、plot()メソッドではインデックスによるチャンネルの選択のみをサポートしている。そのため、レイアウトにはセンサーの種類は含まれておらず、plot()メソッドでは名前や種類による選択はできない。名前や種類による選択は、**mne.pick_channels()関数**または **mne.pick_types()関数**を使用して行うことができる。
<u>tutorial01_05.ipynb</u>のプログラム12では、名前がzで終わるチャンネルを探し、検索されたチャンネルのの電極だけを2D配置図として表示している。

センサーの位置情報を含むRawオブジェクトがある場合は、**mne.channels.make_eeg_layout()関数**または**mne.channels.find_layout()関数**を使用してLayoutオブジェクトを作成できます。
<u>tutorial01_05.ipynb</u>のプログラム13では、RawデータのInfoオブジェクトからレイアウトを作り、作成したレイアウトを表示している。

    ＜注＞
    MEGシステムではセンサーの位置が固定されているため、対応するmake_meg_layout()関数は存在しない。
    したがって、MEGのレイアウトは特定のシステムにおいて一貫性があり、**mne.channels.read_layout()関数**で読み込むか、
    ch_typeパラメータを指定して**mne.channels.find_layout()関数**を使用するだけで済む。

すべてのレイアウトオブジェクトには、レイアウトを .lout または .lay 形式でディスクに書き込む**saveメソッド**があります。.lout 形式と .lay 形式のどちらを選ぶかは、他のアプリケーションでレイアウトファイルをロードする必要がある場合にのみ重要になります（MNE-Python は両方の形式を読み込むことができます）。

### 6,MNE-Pythonの設定

このセッションでは、MNE-Pythonをローカルシステムと分析設定に合わせて構成する方法について説明する。

#### 設定変数の取得と設定

設定変数は、**mne.get_config()関数**および**mne.set_config()関数**を使用して読み書きされる。
特定の設定変数を読み取るには、その名前を**get_config()メソッド**の key パラメータとして渡すことで読み取ることができる（key は最初のパラメータであるため、必要に応じて名前を指定せずに渡すこともできます）。
<u>tutorial01_06.ipynb</u>のプログラム1では、「MNE_USE_CUDA」という設定変数を**mne.get_config()関数**に渡して読み込んでいる。

また、JSONファイルから読み込まれた文字列値は一切解析されないため、**get_config()メソッド**はtrue/falseの設定値であっても Python のブール値ではなく文字列を返すことに注意する必要がある。
同様に、**set_config()関数**は文字列値（または変数を解除するための None 値）のみを設定する。
<u>tutorial01_06.ipynb</u>のプログラム2では、**set_config()関数**に「MNE_USE_CUDA」という設定変数を与えることで、MNEでCUDA（GPU計算）を使う設定を有効にしようとしている。

設定変数が設定されているかどうかわからない場合、それを確認し、存在しない場合のフォールバックを用意する便利な方法がある。**get_config()関数**にはdefaultパラメータが用意されている。
<u>tutorial01_06.ipynb</u>のプログラム3では、設定されてない設定変数「missing_config_key」が本当に設定されていないか確認するために、**defaultパラメータ**を使用して確認を行なっている。

また、**get_config()関数**には 2 つの便利なモードがある。
1つ目は、システム上で設定されているすべての設定変数（およびその値）を含む辞書（dict）を返すモードである。これは key=None を渡すことで実行される（これはデフォルトであるため、省略可能である）。
<u>tutorial01_06.ipynb</u>のプログラム4では、**get_config()関数**のkey パラメータにNoneを渡してシステム上で設定されているすべての設定変数（およびその値）を含む辞書を返している。この時、key=Noneはデフォルトであるため、省略されていることが確認できる。

2つ目の便利なモードは、システム上で設定されているかどうかに関係なく、MNE-Pythonが認識して使用するすべてのキーのリストを返します。これは、キーとして空の文字列 「" "」 を渡すことで行う。
<u>tutorial01_06.ipynb</u>のプログラム5では、**get_config()関数**のkey パラメータに空の文字列 「" "」を渡して、MNE-Pythonが認識して使用するすべてのキーのリストを返している。

認識リストに含まれていない設定変数を追加するには、**set_config()関数**に任意のキーを渡すことで追加することも可能である。ただし、この場合警告が表示される。これは、有効なキーを設定するつもりだったものの、単にスペルを間違えてしまった場合などに役立つチェック機能となる。
<u>tutorial01_06.ipynb</u>のプログラム6では、認識リストに含まれていない設定変数（今回はスペルが間違えている設定変数）「MNEE_USE_CUUDAA」を**set_config()関数**に渡すことで設定変数を追加している。

設定変数を解除するには、**set_config()関数**に**value=None**を指定する。MNE-Pythonにとっては依然として認識されていないキーであるため、警告は表示されますが、キーは解除される。
<u>tutorial01_06.ipynb</u>のプログラム7では、先ほどのスペルが間違えている設定変数「MNEE_USE_CUUDAA」を**set_config()関数**に渡し、**value=None**を指定することで設定変数の削除をしている。

#### 設定が保存される場所

MNE-Pythonは設定変数をJSONファイルに保存される。デフォルトでは、このファイルは Windows では **%USERPROFILE%\.mne\mne-python.json**、Linux または macOS では **$HOME/.mne/mne-python.json** にあります。設定ファイルのフルパスは、**mne.get_config_path()関数**を使用して取得できる。ただし、.mne ディレクトリ内のファイルを直接編集することは推奨されない。もし直接編集する場合は、前のセクションで説明した取得および設定関数を使用してください。
<u>tutorial01_06.ipynb</u>のプログラム8では、**mne.get_config_path()関数**を使用して設定ファイルmne-python.jsonのフルパスを取得している。

ただし、.mne ディレクトリ内のファイルを直接編集することは推奨されない。もし直接編集する場合は、前のセクションで説明した取得および設定関数を使用する。

何らかの理由で別の場所から設定を読み込みたい場合は、**get_config_path()関数**に**home_dirパラメータ**を渡して、読み込みたい設定ファイルが保存されている .mneディレクトリの親ディレクトリを指定することができる。

#### 環境変数を使用する

**環境変数**（Environment Variable）とは、OS（Windows/Linux/macOS）が動作時にプログラムへ提供する「共有の箱」である。
MNE-Cとの互換性を確保するため、MNE-Pythonでは設定を指定するために環境変数の読み書きも行う。これはJSON設定ファイルの読み書きを行うのと同じ関数を使用して行われ、use_envおよびset_envパラメータで制御される。デフォルトでは、get_config()はMNE-PythonのJSONファイルを確認する前にOSの環境変数をチェックする。JSONファイルのみを確認するには、use_env=Falseを指定することで確認できる。
<u>tutorial01_06.ipynb</u>のプログラム9では、MNEの設定ファイル（JSON）には PATH が保存されていないことを確認し、でも環境変数としては存在していることを確認している。これは**mne.get_config()関数**のuse_envパラメータにFalseを与えることで、環境変数は見ずにMNEの設定ファイルだけを確認している。use_envパラメータはデフォルトではTrueであり、このときMNE設定変数に加えOSの環境変数の両方を見ることができる。

また、デフォルトでは、set_config() は JSON ファイルとOSの環境変数の両方に値を設定する。設定変数を JSON ファイルにのみ設定するには、set_env=False を使用します。ここでは、print()ステートメントを使用して、環境変数が作成および削除されていることを確認する。
<u>tutorial01_06.ipynb</u>のプログラム10では、MNEの設定を保存したときに、OSの環境変数（os.environ）にも反映されるかを確認している。ここでは、set_envパラメータにFalseを与えることで、MNEの設定ファイル（JSON）には保存するがOSの環境変数には保存しないという指示を与えている。

#### ログ記録



#### システムの情報を取得する





### 7,mne.reportの利用開始