# MNE Python

## 1章 入門チュートリアル

### 1,MNE-PythonによるMEG/EEG解析の概要

#### データの読み込み
MNE-Pythonはさまざまなデータ形式に対応できる。対応するデータ形式を以下に示す。

- .edf, .bdf, .gdf (European Data Format)
- .cnt (Neuroscan CNT format)
- .fif (Elekta Neuromag FIF format)
- .brainvision (BrainVision format)

※ 本チュートリアルでは、FIFファイルを用いている。

MEG/EEG解析は、まずデータを読み込むことから始める。
データの読み込みは<u>tutorial01_01.ipynb</u>のプログラム1にて行われる。
**mne.datasets.sample.data_path()関数**は、サンプルデータセットのローカルコピーへのパスを取得できる。
想定される場所のいずれにもデータセットが見つからない場合、自動的にデータセットをダウンロードし、データセットへのディレクトリパスを返します。
**mne.io.read_raw_fif()関数**は、生のFIFデータを読み取る機能を持つ。
これによりデータの読み取り、および情報を取得できる。

また、より詳しい情報を取得する場合は、<u>tutorial01_01.ipynb</u>のプログラム2にて取得できる。
これは、データを読み取ったrawをプリントすることで取得している
.infoに関しては、<u>note1.md</u>の4,Infoデータ構造にて説明する。

rawオブジェクトには、さまざまなプロット形式がある。
ここでは、<u>tutorial01_01.ipynb</u>のプログラム3にて示した2つの関数を説明する
**raw.compute_psd()関数**は、センサーデータに対してスペクトル解析を実行する関数である。
**raw.plot()関数**は、生データをプロットする関数である。
このステップはデータの可視化にあたる

#### 前処理
MNE-Python は、さまざまな前処理アプローチがある。
データの前処理では、以下のアプローチがよく使われる。

- フィルタリング
- リサンプリング
- 独立成分分析(ICA)

**フィルタリング**

フィルタリングでは、以下の3つの処理を行う。

- ハイパスフィルタ：低周波数のノイズを除去
- ローパスフィルタ：高周波数のノイズを除去
- ノッチフィルタ：特定の周波数（電源ノイズなど）を除去

ハイパスフィルタ及びローパスフィルタでは**raw.filter()関数**を利用する。この関数は、低周波数のノイズ及び高周波数のノイズを除去することができる。
ノッチフィルタでは**raw.notch_filter()関数**を利用する。この関数は特定の周波数（電源ノイズなど）を除去することができる。
以下にサンプルコードを示す。

```Python
picks = mne.pick_types(raw.info, eeg=True)

# ハイパスフィルタ（1Hz以下をカット）
raw.filter(1, None, fir_design='firwin')

# ローパスフィルタ（40Hz以上をカット）
raw.filter(None, 40, fir_design='firwin')

# ノッチフィルタ（49Hzを除去）
raw.notch_filter(freqs=[49], picks=picks, method='fir')
```

**リサンプリング**

フィルタリングでは、**raw.resample()関数**を使用して、データのサンプリングレートを変更する。
以下にサンプルコードを示す。

````
# 250Hzにリサンプリング
raw.resample(250, npad="auto")
````

**独立成分分析(ICA)**

本チュートリアルでは、独立成分分析(ICA)を用いてデータの処理を行なっている。
独立成分分析(ICA)は、混ざり合った複数の信号（データ）を、それぞれの発生源である「互いに独立な成分」に分離する多変量解析手法である。
具体的な手順は4章にて示すため、ここでは簡単に説明する。

<u>tutorial01_01.ipynb</u>のプログラム4では，ICA（独立成分分析）でノイズ除去を行なっている。
ここでは、**mne.preprocessing.ICA()関数**の説明をしよう。この関数は、ICAの準備を行なっている。具体的には、分解する成分の数、再現性、試行回数を指定している。
<u>tutorial01_01.ipynb</u>のプログラム5では，データにICA（独立成分分析）を適用してデータのノイズを除去している。
ここでは、**raw.load_data()関数**と**ica.apply(raw)関数**について説明する。この関数はデータのロードとicaの適用を行なっている。

#### 実験イベントの検出

データセットには、刺激伝達コンピュータから送信された電気信号を記録した複数の「STIM」チャネル (パルス)が含まれる。
これらのパルスは、このデータセットで**実験イベント（刺激の開始、刺激の種類、参加者の反応（ボタンの押下））**をマークするために使用される。
この「STIM」チャネルを調べることで実験イベントを検出することができる。

<u>tutorial01_01.ipynb</u>のプログラム6では、イベントの検出を行なっている。**mne.find_events()関数**は、生データからイベントを検索している。結果として得られる配列は通常の3列配列で、最初の列にサンプル番号、最後の列に整数型のイベントIDが格納される。
<u>tutorial01_01.ipynb</u>のプログラム7では、イベントの検出に伴ったイベント辞書を作成している。このイベント辞書はEpochs作成を行う時に使用する。
<u>tutorial01_01.ipynb</u>のプログラム8では、イベント検出が期待どおりに機能したことを確認するために、記録期間全体にわたるイベントの分布を視覚化している。これは**mne.viz.plot_events()関数**にて行われる。

#### 連続データのEpochs化

**Epochs**とは、あるイベントを基準に切り出した短い時間データのことである。
Raw オブジェクト（fif形式の生データ）とイベント配列は、Epochs化（Epochsクラスコンストラクタを使用してEpochsオブジェクトを作成する）に必要なものである。このようにしてできたEpochsは、刺激に対する脳の反応を見るために行う。
また、Epochs化を行うと同時にピーク間信号振幅がそのチャンネルタイプにとって妥当な範囲を超えているEpochsはすべて除外している。
Epochs化の手順を参考プログラムとともに説明する。

<u>tutorial01_01.ipynb</u>のプログラム9では、Epochs化で行われるノイズ（ピーク間信号振幅がそのチャンネルタイプにとって妥当な範囲を超えているもの）の除去の設定を行なっている。
<u>tutorial01_01.ipynb</u>のプログラム10では、Epochsオブジェクトの作成及び、設定した基準に沿ったノイズの除去を行なっている。これは、**mne.Epochs()関数**を用いる。
<u>tutorial01_01.ipynb</u>のプログラム11では、左右の刺激提示をまとめて、比較できるようにしている。これは**equalize_event_counts()関数**によって行われている。この関数は、各条件からエポックをランダムにサンプリングし、有効なエポックが最も少ない条件のエポック数に合わせる機能を持っている。これをはじめに使うことで、思考数を整え比較できるようにしている。
<u>tutorial01_01.ipynb</u>のプログラム12では、データのプロットを行なっている。Epochsオブジェクトも、Rawオブジェクトと同様にさまざまなプロット方法がある。本チュートリアルでは、**plot_image()関数**を用いている。この関数は、各エポックをイメージマップの1行として表示し、色で信号強度を表している。

#### 時間周波数解析

**時間周波数解析**とは、信号の周波数成分が時間とともにどう変化するかを分析する手法である。
時間周波数解析ツールは、時間周波数表現、パワースペクトル密度、およびクロススペクトル密度を計算するための複数のアルゴリズムの実装を提供する。基本的な手法として短時間フーリエ変換（STFT）や、ウェーブレット変換を用いる。

<u>tutorial01_01.ipynb</u>のプログラム13により、データの時間周波数表現を計算しプロットしている。この機能を実現しているのが、**aud_epochs.compute_tfr()関数**である。この関数によりエポック化されたデータの時間周波数表現を計算している。

#### 誘発応答の推定

**誘発応答**とは、外部刺激によって誘発される脳や神経の電気的活動（電位）のことである。
この値を調べることで、視覚、聴覚、体性感覚（触覚など）の経路が正常に機能しているか、脳幹部や神経経路の病変を検査することができる。この誘発応答の推定値は、各条件のエポックを平均化することで、聴覚刺激と視覚刺激に対する誘発応答の推定値を得ることができます。ここで各条件は、実験のパターン（反応の種類）ごとのデータを示す。

<u>tutorial01_01.ipynb</u>のプログラム14に示すもので、誘発応答の推定値を得ることができる。**mne.viz.plot_compare_evokeds()関数**は、1つ以上の条件およびチャネルについて、誘発された時間経過をプロットできる。これは、Epochs オブジェクトに対して average メソッドを呼び出した後に利用することで使用できる。
また、<u>tutorial01_01.ipynb</u>のプログラム15に示すように、**plot_joint()関数** や**plot_topomap()関数** などの他のプロット手法を使用することで、各誘発オブジェクトについてより詳細な情報を得ることができます。
さらに、<u>tutorial01_01.ipynb</u>のプログラム16に示すように、**mne.combine_evoked()関数**を使用することで、誘発波形を組み合わせ、条件間の対比を示すこともできる。weights=[1, -1] を指定することで、単純な差分波形を生成でき、その後**plot_topo()関数**を使用して、各センサーにおける差分波形をプロットしている。

#### 逆モデリング

**逆モデリング**とは、観測結果からその原因となるパラメータや構造を推定する手法である。
これは、センサーデータをこの被験者のソース空間（構造MRIスキャンによって推定された、被験者の皮質表面上または皮質体積内の点の集合）に投影することで、誘発活動の起源を推定できます。MNE-Pythonの逆モデリングの手法には、以下の手法がある。

- 動的統計的パラメトリックマッピング
- 双極子フィッティング
- ビームフォーマー

※ 本チュートリアルでは、最小ノルム推定（MNE）を用いている。

**最小ノルム推定（MNE）**とは、線形逆演算子を使用して、EEG+MEGセンサー測定値をソース空間に投影する手法です。これにより皮質表面に制約された活性化の連続マップを生成できる。この処理は、<u>tutorial01_01.ipynb</u>のプログラム17に示す。このプログラムでは、**mne.minimum_norm.read_inverse_operator()関数**によりFIFファイルから逆演算子分解を読み込む処理を行い、後に**mne.minimum_norm.apply_inverse()関数**で誘発データに逆演算子を演算している。
この結果をもとに、<u>tutorial01_01.ipynb</u>のプログラム18では、逆モデリングの結果を脳の3D表示としてプロットしてる。



### 2,データの変換

MNE-Python の多くのデータオブジェクト ( Raw、Epochs、Evokedなど) には、データをその場で変更するメソッドがある。これは、**大規模なデータセットを扱う場合に、計算に必要なコンピュータメモリの量を削減できるため**に行う。しかし、それが行われていることを認識していないと、予期しない結果につながる可能性があります。このチュートリアルでは、インプレース処理の例と、それを回避する方法とタイミングについて説明する。

#### 信号処理

ほとんどのMNE-Pythonデータオブジェクトには、ハイパス、ローパス、バンドパスフィルタ(**filter()関数**)、バンドストップフィルタ(**notch_filter()関数**)、ヒルベルト変換(**apply_hilbert()関数**)など、信号処理(**フィルタリング**)のための組み込みメソッドがあります。これらは通常、データをその場で変更するため、比較のために未処理のデータを保持したい場合は、まずそのコピーを作成する必要がある。

<u>tutorial01_02.ipynb</u>のプログラム1では、実際の信号処理のプログラミングの例を示す。ここでは、**raw.apply_hilbert()関数**を用いてヒルベルト変換をしている。

#### チャンネル選択

データをその場で変更するもう1つの手法は、**チャンネル選択手法**がある。

<u>tutorial01_02.ipynb</u>のプログラム2では、実際の信号処理のプログラミングの例を示す。ここでは、**original_raw.pick()関数**にてチャンネルを変換している。また、EEGチャンネルのみを選択する場合、磁力計チャンネルがなくなるため、磁力計のみに影響を与えるプロジェクターは除外されるため注意が必要となる。

#### コピーパラメータ

信号処理では、処理前後のデータの比較のために前データのコピーを作成する必要があると示した。しかし、MNE-Python の特定の関数には**copyパラメータ**としてこの機構が組み込まれている。具体的にはデータの変更済みコピーを返す**copy=True**または、その場で操作する**copy=False**を指定できる。

<u>tutorial01_02.ipynb</u>のプログラム3では、実際のcopyパラメータを利用した例を示す。ここでは、copyパラメータが組み込まれている**mne.set_eeg_reference()関数**を用いた。これはリファレンス処理を行なったデータをプロットしている。しかし、copy=Trueを指定しているため、リファレンス処理を行なったデータには影響が及んでいない。

分析の探索段階で、さまざまなデータクレンジング手法の効果を試してみたい場合がある。計算を変更するたびにデータを再読み込みしたり、以前の手順を繰り返したりする手間を省きたい場合は、**raw.copy().filter(...).plot()**や**raw.copy().apply_proj().compute_psd().plot()**といったパターンを念頭に置いとくことが必要である。

### 3,生データからイベントを解析

<u>1,MNE-PythonによるMEG/EEG解析の概要</u>の実験イベントの検出では、STIMチャネルから実験イベントを読み取る例を見ていた。
ここでは、イベントとアノテーションについてより広く説明し、STIMチャネルからの読み取りに関する詳細情報を提供するとともに、マーカーファイルにあるイベント、またはデータファイルに埋め込み配列として含まれているイベントを読み取る例を示す。

#### イベントとアノテーションのデータ構造

一般的に、**イベント**と**アノテーション**というデータ構造は、どちらも同じ目的を持っている。これらは、EEG/MEG記録中の時刻と、その時刻に何が起こったかという記述とを関連付けるものである。ここから、イベントとアノテーションの違いについてみていく。

|項目|イベント|アノテーション|
|:---|:---:|:---:|
|単位|「いつ」をサンプル単位で表現する|「いつ」を秒単位で表現する|
|記述の制限|「何」を整数である「Event ID」コードで表現する|「何」を文字列で表現する|
|持続時間の表現方法|Event配列内のイベントには持続時間が含まれていない|Annotationsオブジェクトの各要素には必ず持続時間が含まれている|
|内部表現|通常のNumPy配列として格納される|MNE-Pythonで定義されたリストのようなクラス|

#### STIMチャンネルとは

**STIMチャンネル（「刺激チャンネル」の略）**とは、EEG、MEG、またはその他のセンサーからの信号を受信しないチャンネルである。
代わりに、STIMチャンネルは、**刺激の開始や被験者によるボタン押下反応などの実験イベントに同期した電圧**（通常は実験制御コンピューターから送信される、一定の振幅を持つ短い矩形直流パルス）を記録する（これらのパルスは、 TTLパルス、イベントパルス、トリガー信号、または単に「トリガー」と呼ばれることもある）。場合によっては、これらのパルスは厳密には実験イベントに同期せず、次の試行で発生する刺激の種類（または実験条件）を示すために、試行間に発生することもある。

**DCパルス**とは、電圧のハイとローを高速で切り替え、一定間隔でオン／オフを繰り返す直流電力である。
このDCパルスが、すべて1つのSTIMチャネルに集中している場合もあれば、複数のチャネルに分散している場合もある。前者の場合、異なる実験イベントや試行タイプは異なる電圧の大きさとして符号化され、後者の場合、パルスが発生するチャネルを利用して、異なるイベントや条件を符号化することができる。複数のSTIMチャンネルを備えたシステムであっても、デコードを行うために、他のSTIMチャンネルの重み付けされた合計を記録するチャンネルが1つ設けられていることが多く、そのチャンネル上の電圧レベルを特定のイベントタイプとして明確にデコードできるようにしている。
また、旧型のNeuromagシステム（サンプルデータの記録に使用されたものなど）では、この「加算チャンネル」は通常STI 014でしたが、新しいシステムではSTI101が一般的である。

<u>tutorial01_03.ipynb</u>のプログラム1では、実際に生データファイルのSTIMチャンネルを確認することができる。この結果から、STI 014（加算チャンネル）には大きさの異なるパルスが含まれているのに対し、他のチャンネルのパルスは一貫した大きさであることがわかる。また、他のSTIMチャンネルのいずれかにパルスが発生するたびに、STI 014にも対応するパルスが発生していることも確認できる。

#### STIMチャネル信号をイベント配列に変換する

データにSTIMチャネルで記録されたイベントが含まれている場合、**find_events()関数**を使用してそれらをイベント配列に変換できます。
各パルスのオンセット（またはオフセット）のサンプル番号がイベント時刻として記録され、パルスの振幅は整数に変換される。これらのサンプル番号とイベント整数コードのペアは、**NumPy配列**（通常「**イベント配列**」または単に「**イベント**」と呼ばれます）に格納される。この関数の最も単純な形式では、Rawオブジェクトと、イベントを読み取るチャネルの名前のみで利用できる。

<u>tutorial01_03.ipynb</u>のプログラム2では、STIMチャンネルで記録されたイベントをイベント配列に変換している。この結果から左の列にサンプル番号、右の列にイベント整数コードがペアとなって格納されていることがわかる。

    ＜イベント配列の中央の列＞
    MNE-Pythonイベントは実際には3つの値で構成されていて、サンプル番号と整数イベントコードの間に**0**が格納されていることが確認できる。
    この値は、直前のサンプルにおけるイベントコードを示す値である。実際には、この値はほぼ常に「0」ですが、
    1つのサンプルよりも長い期間のイベントの終了点を検出するために使用される。



#### 埋め込みイベントを注釈として読み込む



#### イベント配列とアノテーションオブジェクト間の変換



#### アノテーションごとに複数のイベントを作成する



### 4,Infoデータ構造



### 5,センサー位置の操作



### 6,MNE-Pythonの設定



### 7,mne.reportの利用開始